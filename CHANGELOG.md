# 更新日志

## v1.7.6

### 更新内容

- 修正偏好设置窗口左上角交通灯按钮与侧栏边框的视觉重叠，侧栏整体下移并保留更稳定的窗口拖拽区域。
- 将侧栏调整为更冷调的磨砂透明背板，降低灰色感，同时恢复浮起圆角卡片阴影。
- 重新实现侧栏选中态圆角背景，确保当前项目有清晰可见的浅蓝高亮和描边。
- 将侧栏和设置内容字号恢复到中号层级，避免过小造成的阅读压力。
- 保持现有功能逻辑不变，仅调整偏好设置窗口 UI 视觉细节。

### 验证

- 已通过本地 `swiftc -typecheck` 类型检查。
- 已通过偏好设置窗口预览截图检查交通灯避让、侧栏磨砂底色、圆角阴影和选中态。
- 已通过安全与性能相关代码审查，确认本次改动未引入新的网络、文件写入或权限调用路径。
- 已通过本地 `./build.sh` 构建、签名验证、Universal Binary 检查和 DMG 校验。

## v1.7.5

### 更新内容

- 将偏好设置左侧导航收回到 AppKit 原生 `NSTableView.sourceList` 的侧栏密度，减少自绘感。
- 使用原生 `NSVisualEffectView.sidebar` 作为侧栏磨砂底，缩小侧栏宽度、行高、图标和字体。
- 将选中态改为更接近 Finder 的浅色圆角行背景，不再使用过大的按钮式选中块。
- 降低右侧设置卡片和侧栏的阴影强度，减少卡片边缘的脏影和层叠感。
- 继续保持功能逻辑不变，仅调整偏好设置窗口视觉呈现。

### 验证

- 已通过本地 `swiftc -typecheck` 类型检查。
- 已通过偏好设置窗口截图预览检查侧栏密度、右侧卡片阴影和布局位置。
- 已通过本地 `./build.sh` 构建、签名验证、Universal Binary 检查和 DMG 校验。

## v1.7.4

### 更新内容

- 修正偏好设置窗口右侧内容区域的布局锚点，内容列固定从左上角开始排布，避免设置分组漂到右下角。
- 将窗口背板、侧栏和设置分组调整为更克制的磨砂透明效果，不再使用大片死白底色。
- 将左侧导航恢复为有外边距、圆角和阴影的浮起侧栏卡片，并补齐内部圆角遮罩。
- 重做右侧分组和设置行的等宽约束，说明文字、控件和分隔线按 BetterDisplay 风格自然对齐。
- 将自动化 “App 列表” 改为单独的大输入行，输入框横向占满可用宽度，便于填写多个 App 名称和 Bundle ID。
- 保持现有功能逻辑不变，仅修正偏好设置窗口视觉和布局。

### 验证

- 已通过本地 `swiftc -typecheck` 类型检查。
- 已通过 `git diff --check` 空白检查。
- 已通过本地 `./build.sh` 构建，生成 Universal Binary 与 DMG 安装包。

## v1.7.3

### 更新内容

- 将偏好设置窗口整体重塑为更接近 BetterDisplay 的视觉风格：浅色毛玻璃侧栏、白色内容区、宽松的大尺寸导航和灰色胶囊选中态。
- 重新调整设置页结构表现，移除旧版页头图标与副标题，改为更直接的页面标题和大字号分组标题。
- 重新设计设置分组卡片，使用浅灰圆角面板、内嵌分隔线、彩色 SF Symbol 图标徽章和更强的标题/说明层级。
- 重做关于页布局，改为居中的大图标、应用名称、版本信息和项目链接，更接近 BetterDisplay 的关于页面风格。
- 保留现有偏好项、开关、菜单栏设置、自动化、电池保护、通知和更新检查逻辑，仅调整视觉呈现。

### 验证

- 已通过本地 `swiftc -typecheck` 类型检查。
- 已通过 `git diff --check` 空白检查。
- 已通过本地 `./build.sh` 构建，生成 Universal Binary 与 DMG 安装包。

## v1.7.2

### 更新内容

- 再次增强偏好设置窗口 Liquid Glass 可读性，侧边栏、内容区和设置分组加入更稳定的明暗动态底色，降低背景文字透出干扰。
- 微调偏好设置窗口的最小尺寸、标题字号、设置行高度、图标徽章和侧边栏选中态，让整体界面更简洁、现代、易扫读。
- 优化菜单栏状态刷新，复用状态图标和倒计时 Timer 创建逻辑，减少常驻运行时的重复对象创建。
- 优化自动化检测性能，仅在启用相关规则时轮询；全屏检测保持快速响应，电源检测降低到低频刷新。
- 加固更新检查，只接受 Keep Bright 仓库下可信的 GitHub HTTPS 发布和下载链接，并清洗请求头中的版本字段。
- 加固全局快捷键注册和 DMG 生成脚本，补充系统 API 返回值处理，并避免 AppleScript 直接拼接路径参数。
- 加固 GitHub Actions Release 流程，checkout 不再持久化仓库凭证。

### 验证

- 已通过本地 `swiftc -typecheck` 类型检查。
- 已通过 `zsh -n build.sh` 和 `zsh -n Tools/create_dmg.sh` 脚本语法检查。
- 已通过 `git diff --check` 空白检查。
- 已通过本地 `./build.sh` 构建，生成 Universal Binary 与 DMG 安装包。

## v1.7.1

### 更新内容

- 优化偏好设置窗口视觉层次，窗口尺寸、标题区、侧边栏和内容区留白更接近新版 macOS 设置界面。
- 页面标题区新增当前设置页的 SF Symbol 图标，让不同设置页更容易识别。
- 设置项新增轻量图标徽章，常规、自动化、计时、电池、更新、通知和关于页面的控件层次更清晰。
- 调整 Liquid Glass 分组的 tint、可读性底色、边框和圆角，降低“太平”和“太透”的观感。
- 优化侧边栏行高、间距和选中态，让导航更精致。
- 操作按钮新增图标，GitHub、系统设置和隐私说明入口更符合 macOS 工具按钮风格。
- 增强 DMG 生成脚本的 detach 和 convert 重试逻辑，降低 GitHub Actions 里镜像短暂占用导致发布失败的概率。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。
- 已验证 DMG 安装包可以通过 `hdiutil verify` 校验。

## v1.7.0

### 更新内容

- 新增“自动化”偏好设置页，可独立开启指定 App、全屏、外接显示器和连接电源自动保持亮屏。
- 新增指定 App 规则，支持按前台 App 名称或 Bundle ID 匹配，默认提供 Keynote、PowerPoint、Zoom、腾讯会议、QuickTime Player 等常见演示和会议应用。
- 新增全屏窗口检测，进入全屏演示、观影或会议时可自动开启保持亮屏。
- 新增外接显示器检测，连接外接屏或投影仪时可自动开启保持亮屏。
- 新增电源适配器检测，插电时可自动开启，拔电后按规则恢复。
- 菜单栏面板新增自动化状态显示，可以直接看到当前触发规则或暂停状态。
- 自动化开启后会在触发条件结束时自动关闭；如果用户手动关闭，会暂停当前自动化触发直到条件结束，避免反复抢回控制。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。
- 已验证 DMG 安装包可以通过 `hdiutil verify` 校验。

## v1.6.4

### 更新内容

- 降低偏好设置窗口右侧内容区的透明度，解决背景过亮时文字不清晰的问题。
- 将右侧内容玻璃从 `.clear` 调整为更稳定的 `.regular` 样式。
- 为内容区和设置分组增加动态可读性底色，保留 Liquid Glass 质感同时提升文字对比度。
- 加强设置分组卡片的玻璃 tint，避免控件和说明文字融入桌面背景。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。

## v1.6.3

### 更新内容

- 固定 GitHub Actions Release 构建环境为 `macos-26`，确保自动发布流程可以使用 macOS 26 Liquid Glass API 构建安装包。
- 保留 v1.6.2 的 Liquid Glass 偏好设置界面改动。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。

## v1.6.2

### 更新内容

- 将偏好设置窗口升级为 macOS 26 原生 Liquid Glass 视觉。
- 使用 `NSGlassEffectView` 渲染侧边栏、内容背景和设置分组。
- 使用 `NSGlassEffectContainerView` 合并玻璃层，降低多块玻璃视图的渲染成本。
- 操作按钮改为 macOS 26 的 glass bezel 样式。
- 关于窗口文案同步为 Liquid Glass 偏好设置。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。

## v1.6.1

### 更新内容

- 重新设计偏好设置窗口，改为更接近 macOS 系统设置的左侧栏布局。
- 使用原生 `NSVisualEffectView`、source list、SF Symbols 和透明标题栏，让窗口视觉更贴近新版 macOS。
- 将设置中的开关项从复选框改为原生 `NSSwitch`。
- 设置项改为分组卡片式布局，常规、计时、电池、更新、通知、关于各页更容易浏览。
- 通知设置页新增“打开系统设置”入口，方便调整通知预览显示。
- 关于窗口改为从 `Info.plist` 动态读取版本号，减少后续发版时遗漏版本文案。

### 验证

- 已通过本地 `./build.sh` 构建。
- 已验证 Universal Binary 同时包含 `arm64` 和 `x86_64`。
- 已验证应用签名有效。

## v1.6.0

### 更新内容

- 新增全局快捷键 `Option-Command-K`，可在任意应用中快速开启或关闭保持亮屏。
- 新增菜单栏显示模式：只显示图标、显示剩余时间、显示防睡眠模式、显示状态文字。
- 新增定时快速延长 15 分钟、延长 30 分钟和重新开始计时。
- 新增通知偏好，可分别控制状态、计时和电池保护通知。
- 偏好设置拆分为常规、计时、电池、更新、通知、关于多个页面。

### 验证

- 已通过本地构建、签名、DMG 校验和运行时防息屏断言检查。
